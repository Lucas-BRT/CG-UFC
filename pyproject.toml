[project]
name = "atividade-01"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lucas-brt", email = "lucasbritodepaula@proton.me" }
]
requires-python = ">=3.8"
dependencies = [
    "glfw>=2.5.0",
    "numpy>=1.24.4,<2; python_version < '3.9'",
    "numpy>=1.24.4; python_version >= '3.9'",
    "pyopengl>=3.1.6",
]

[project.scripts]
atividade-01 = "atividade_01:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
